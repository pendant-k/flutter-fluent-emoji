/// Main Logic for convert String emoji to Flutter Emoji Widget
library;

class EmojiConverter {
  /// Convert emoji to Flutter Emoji Widget
  String convert(String emoji) {
    const convertedUrl = "";

    return convertedUrl;
  }
}
